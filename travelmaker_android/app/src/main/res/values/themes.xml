<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Travelmaker.DayNight" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/main_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/text_main_color</item>
    </style>
    <style name="Base.Theme.Travelmaker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/main_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/text_main_color</item>
    </style>

    <style name="Theme.Travelmaker" parent="Base.Theme.Travelmaker" />
</resources>